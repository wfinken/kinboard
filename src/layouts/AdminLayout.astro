---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
const session = Astro.locals.session;

const navItems = [
  { href: '/admin', label: 'Overview' },
  { href: '/admin/calendars', label: 'Calendars' },
  { href: '/admin/chores', label: 'Chores' },
  { href: '/admin/meals', label: 'Meals' },
  { href: '/admin/notes', label: 'Notes' },
  { href: '/admin/layout', label: 'Layout' },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} · KinBoard Admin</title>
  </head>
  <body class="min-h-screen bg-neutral-950 text-neutral-100 font-sans">
    <div class="mx-auto max-w-4xl px-4 pb-16">
      <header class="border-b border-neutral-800 py-4">
        <div class="flex items-center justify-between gap-3">
          <a href="/admin" class="shrink-0 text-lg font-semibold">
            KinBoard Admin
          </a>
          <div class="flex shrink-0 items-center gap-3">
            {session?.user && (
              <span class="hidden max-w-40 truncate text-sm text-neutral-400 sm:inline">
                {session.user.email}
              </span>
            )}
            <a
              href="/api/auth/signout"
              class="rounded-md bg-neutral-800 px-3 py-2 text-sm hover:bg-neutral-700"
            >
              Sign out
            </a>
          </div>
        </div>
        <nav class="-mx-4 mt-4 flex gap-1 overflow-x-auto px-4 pb-1 sm:mx-0 sm:px-0">
          {
            navItems.map((item) => (
              <a
                href={item.href}
                class="shrink-0 rounded-md px-3 py-2.5 text-sm text-neutral-300 hover:bg-neutral-800 hover:text-white"
              >
                {item.label}
              </a>
            ))
          }
        </nav>
      </header>
      <main class="py-8">
        <h1 class="mb-6 text-2xl font-bold">{title}</h1>
        <slot />
      </main>
    </div>
  </body>
</html>
